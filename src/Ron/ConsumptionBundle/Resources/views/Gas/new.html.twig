{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gas creation</h1>

    <form action="{{ path('gas_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('gas') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
