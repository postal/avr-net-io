{% extends '::base.html.twig' %}

{% block content %}
    <h1>Gas list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Verbrauch</th>
                <th>Createdat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.verbrauch }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gas_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gas_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gas_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
