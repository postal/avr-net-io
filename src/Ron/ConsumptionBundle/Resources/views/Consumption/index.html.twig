{% extends '::base.html.twig' %}

{% block content %}
    <h1>Verbrauchsliste</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Water</th>
                <th>Gas</th>
                <th>Energy</th>
                <th>Createdat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in energy %}
            <tr>
                <td><a href="{{ path('energy_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.value }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('energy_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('energy_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('energy_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
